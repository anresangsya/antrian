<!DOCTYPE html>
<html lang="en">
<head>
	<% include head %>
</head>
<body style="background-color:white;">
	<% include navbar %>

	<div class="container">
		<div class="row">
			<h1 align="center" height="30px"><b> <%= title %> </b></h1>
			<h2><%= result[0].id_poli  %></h2>
			
			<% result.forEach(function(result) { %>

			<div class="col-sm-3">
				<div class="well">
					<h3 align="center">Poli <%= result.nama_poli %> </h3>
					<img src="<%= result.icon %>" class="img-responsive" alt="Image" style="width: 50%; margin-top: 10px; margin-left: 55px;">
					<a href="detailPoli.php" class="btn btn-success" role="button" style="margin-top: 25px; margin-left: 70px;">Lihat Poli</a>
				</div>
			</div>

			<% }); %>
		</div>
		<hr>
	</div>
</body>
</html>